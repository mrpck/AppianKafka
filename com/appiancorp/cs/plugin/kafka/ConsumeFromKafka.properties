name=Consume from Kafka

storeKey.missing=Missing secured credential store which is required if using SASL
storeDocs.missing=Mssing truststore and keystore docs which are required if using SSL
securityProtocol.invalid=Invalid security protocol
saslMechanism.invalid=Invalid sasl mechanism
numConsumers.high=Too many consumers, must be 5 or fewer
numConsumers.low=Too few consumers, must be 1 or greater
maxRuntime.high=Too low max runtime, must be <= 58 minutes
maxRuntime.low=Too low max runtime, must be >= 1 minute
pollingInterval.high=Too high polling interval, must be <= 300000ms (5 minutes)
pollingInterval.low=Too low polling interval, must be >= 1000ms (1 second)
sleepingInterval.high=Too high sleeping interval, must be <= 1800000ms (30 minutes)
sleepingInterval.low=Too low sleeping interval, must be >= 1000ms (1 second)
messageFilter.invalid=Invalid JSON path expression
sessionTimeout.low=Too low session timeout, must be >= 10000ms (10 seconds)
